{
  "name": "fullstackserver",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../fullstackopen/part2/phonebook/ && npm run build && cp -r build ../../../fullstackserver",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",    
    "logs:prod": "fly logs"

  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iltan2/fullstackserver.git"
  },
  "author": "Israel Tan",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/iltan2/fullstackserver/issues"
  },
  "homepage": "https://github.com/iltan2/fullstackserver#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
